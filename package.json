{
  "name": "first-system",
  "version": "2.0.0",
  "description": "CSS compiler for the FirstSystem system",
  "scripts": {
    "build": "sass src/scss/first-system.scss css/first-system.css --style=expanded --no-source-map",
    "watch": "sass src/scss/first-system.scss css/first-system.css --style=expanded --source-map --watch"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Asacolips",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "sass": "^1.53.0"
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  }
}
